kind = "web"
previewPath = "/"
title = "LearnSync 3.0"
version = "1.0.0"
id = "artifacts/learnsync"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19943

[services.development]
run = "pnpm --filter @workspace/learnsync run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/learnsync", "run", "build" ]
publicDir = "artifacts/learnsync/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19943"
BASE_PATH = "/"
